server:
  port: 9999

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_booking_system?createDatabaseIfNotExist=true
    username: root 
    password: 
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect